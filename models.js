class AppModel {

    cameraPosition = [0,0]


}


class PlotModel {

    constructor(paths, position, scale) {
        this.position = position
        this.paths = paths
        this.scale = scale
    }
}